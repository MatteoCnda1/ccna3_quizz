# ccna3_quizz

pour lancer en local créez un env virtuel python et lancer dasn le repot la commande suivante :

python3 -m http.server <nbr_port>

pour le nombre du port prenez un port libre pas celui de ssh par exemple ça ne marchera pas !
